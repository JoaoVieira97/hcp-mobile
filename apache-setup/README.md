# Apache2 setup and run

Install Apache2 (Ubuntu):

```bash
sudo apt-get install apache2
```

___
Run `bash` script using **start** or **stop** options:
```bash
sudo ./run.sh start
sudo ./run.sh stop
```
